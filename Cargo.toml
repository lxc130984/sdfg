[package]
name = "myweb"
version = "0.1.0"
edition = "2024"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
[lib]
crate-type = ["cdylib"]  # 这行很重要！告诉 Rust 生成动态库

[dependencies]
wasm-bindgen = "0.2.114"
web-sys = { version = "0.3", features = [
    "console",  # 启用 console 功能
] }